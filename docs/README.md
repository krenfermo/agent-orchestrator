# agent-orchestrator rewrite docs

The agent-orchestrator is being rebuilt as a long-running Go backend daemon
(`backend/`) plus an Electron + TypeScript frontend (`frontend/`). The backend
supervises coding-agent sessions and exposes daemon control, project/session
state, terminal streaming, and CDC/event infrastructure.

Start with [architecture.md](architecture.md) for the current backend model and
[cli/README.md](cli/README.md) for the CLI surface.

## Reference docs

| Doc                                                    | What it covers                                                                                                        |
| ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------- |
| [architecture.md](architecture.md)                     | Current backend model, package layout, status derivation, persistence/CDC, and load-bearing rules.                    |
| [backend-code-structure.md](backend-code-structure.md) | Package ownership rules for the Go backend: domain, services, ports, adapters, storage, HTTP, CLI, and daemon wiring. |
| [cli/README.md](cli/README.md)                         | CLI commands and daemon control surface.                                                                              |
| [cloud-development.md](cloud-development.md)           | Optional private checkout workflow, current Cloud foundation, remaining implementation, and recommended build order. |
| [cloud-refactor.md](cloud-refactor.md)                 | Public contracts, generated Cloud schema types, typed client, reusable product UI, and private implementation boundaries. |
| [code-graph.md](code-graph.md)                         | Provider-agnostic code-graph adapter boundary, the native AST/symbol indexer, hash-gated incremental updates, and per-project index isolation under `~/.ao`. |
| [context-router.md](context-router.md)                 | Role-aware context router: per-role section ordering and token budgets, progressive compact/expanded retrieval, hard-cap enforcement, and the feature flag that keeps dispatch on full context by default. |
| [p1-autonomy-readiness.md](p1-autonomy-readiness.md) | The P1 INTEGRATION document: how the strategy, recovery/repair, capacity, placement and provider-failover pieces compose into one autonomous system; the per-task placement override and the generation-safe placement transition; the terminal placement retirement and the `Runtime.Destroy` ABA boundary; the operator observability recipe; the validation evidence; and the known limitations and GO criteria. |
| [p1d-placement-locks-and-failover.md](p1d-placement-locks-and-failover.md) | Execution placement, branch authority and the generation-safe branch-lock cession that lets a Repair Agent work on a direct branch; worker runtime ownership tokens; placement GC; and the named provider-failover safety model. States plainly which parts pre-date P1-D and which remain unimplemented. |
| [p1c-capacity-and-runtime-gc.md](p1c-capacity-and-runtime-gc.md) | The runtime capacity scheduler (durable claims, generation fencing, limits, queue order and fairness) and Runtime GC (ownership/incarnation/terminality proofs, orphan classification, triggers) — and why runtime capacity is a different thing from provider capacity. |
| [p1b-recovery-and-repair.md](p1b-recovery-and-repair.md) | The recovery assessment, obligation-driven Resume, plan reuse/revision authority, and the bounded Repair Agent: deterministic eligibility, generation/single-flight rules, the frozen repair policy, parent/child convergence, and the UI contract that the frontend renders the assessment rather than deciding safety. |
| [execution-strategy.md](execution-strategy.md)         | The canonical Task/Autonomous/Master execution-strategy model and the separate Automatic/Manual approval policy: when each is used, the deterministic AUTO rules, how a selection is frozen and proven, master child bounds, and legacy compatibility. |
| [development.md](development.md)                       | Prerequisites, build steps, running tests, and troubleshooting for local development.                                 |
| [STATUS.md](STATUS.md)                                 | What is shipped on `main` today and what is still in flight.                                                          |
| [workflow-lifecycle-mapping.md](workflow-lifecycle-mapping.md) | Canonical mapping from persisted workflow-engine tables to the UI lifecycle vocabulary; contract for the backend projection and workflow UI. |
| [worker-lifecycle-audit.md](worker-lifecycle-audit.md) | Audit of the full durable path from objective/plan creation through plan approval, task dispatch, worker launch/confirmation/running and completion evidence to the review transition: every durable write point, every crash window between adjacent writes, the ambiguous states each produces, and the generation-conditioned CAS model proposed to close them. |
| [review-fix-delivery-integrity.md](review-fix-delivery-integrity.md) | The durable review→fix path end to end, the non-secret evidence AO records for every fix cycle (findings digest/count, embedded flag, receipt match, attempt binding), and incident wf-a816d7fe: an unbracketed tmux paste that shredded a fix prompt into per-line submissions. |
| [stack.md](stack.md)                                   | Accepted library/runtime choices, pending stack decisions, and dependencies explicitly avoided for V1.                |
| [telemetry.md](telemetry.md)                           | Telemetry collection, privacy safeguards, and configuration.                                                           |
| [posthog-cost-controls.md](posthog-cost-controls.md)   | PostHog event-name migration, ingestion drop rules, and dashboard queries for reducing telemetry spend.              |
| [context-router-metrics.md](context-router-metrics.md) | Context-selection metrics recorded alongside the baseline evidence (potential/selected/reused/new sizes, measured vs estimated) and the disabled-vs-enabled harness that fails the build when routing changes an outcome. |
| [project-memory-baseline.md](project-memory-baseline.md) | Phase 0 project-memory baseline: the per-dispatch context/token evidence schema, its measured/estimated/unavailable labeling rule, and the harness that produces it. |
| [project-memory-store.md](project-memory-store.md) | Durable project memory: the item schema and its provenance fields, where items live under `~/.ao`, the content-hash idempotent upsert, staleness against HEAD/file hashes, and ingesting the baseline evidence. |
| [project-memory.md](project-memory.md) | P2-A durable project memory: the cache-vs-source-of-truth rule, the item/relation schema with generation-conditioned CAS, the bounded restart-safe indexer and its incremental change-set path, drift detection and the four states, role-specific bounded context packs, multi-repo/worktree and task-local semantics, the Grae/Graphify port with its local default, the measured baseline, and the `workflow_mutation_provenance` deferral. |
| [project-memory-optimization.md](project-memory-optimization.md) | P2-B: project memory in the normal execution cycle. The three rollout modes and why a boolean was not enough, the lifecycle trigger and its single-flight, the digest-ledger fix for the planner's repeated rescan, per-role budgets and their eviction order, provable dedupe with coverage-first selection, the authority-keyed pack cache, failure fallbacks, and the MEASURED before/after (planner AO-assembled context 87,209 → 40,378 bytes, −53.7%) with an explicit statement of what those numbers do not cover. |
| [shared-task-knowledge.md](shared-task-knowledge.md) | P2-C: making what one task learns reusable by the next one. The lifecycle carried on the P2-A item rather than a second store, the three sharing scopes (task/workflow/canonical) and the sibling-safety rule they encode, promotion authority for direct-branch vs isolated-worktree work, decision supersession and conflict marking, risk resolution, the evidence-based relevance gate that keeps prior task history away from unrelated work, per-scope compaction, durable context manifests, and the MEASURED related-vs-unrelated impact (same-module task 6,203 pack bytes with 32 shared facts; unrelated task 1,450 bytes with 0, from the same 32 candidates). |
| [usage-accounting.md](usage-accounting.md) | P3-E: tokens, cost and AO-assembled context. What each figure is entitled to claim (provider-reported vs estimated vs unknown, and why a cost may be `unknown` but never `$0.00`); role attribution as a durable time window over one session, which is what stops a repair delivered into the worker's own session from being counted twice; the backend-owned rate card and its operator override; budgets enforced only at safe boundaries and never mid-response; and the rule that AO-assembled context is never added to, or compared as equal with, a provider's input tokens. |
| [project-memory-authority.md](project-memory-authority.md) | P2-D memory integrity, drift and provenance: the two-axis model (`state` for evidence, `authority` for licence) and why serving requires both, the `workflow_mutation_provenance` production writer with its five mutation boundaries and its exactly-once generation-safe discipline, the direct-branch and worktree-integration promotion proofs (and why a verified worktree is not canonical), repository identity beyond the path, drift v2 with proven-rename knowledge carry, supersession that does not resurrect predecessors, legacy classification, graph edge retirement, the fail-closed/fallback table, and the `ao memory validate` / `ao memory provenance` diagnostics. |
| [p2-project-memory-audit.md](p2-project-memory-audit.md) | P2 audit of every current context source for the Planner, Worker, Reviewer, Diagnostic Agent and both Repair Agents, with file/package references: AO-assembled vs agent-assembled context, system prompts and project `AgentRules`, git diff/history usage split into AO lifecycle reads vs agent-initiated inspection, what is rescanned per task vs durable/cacheable, the explicit Graphify/Grae finding, and which builders can consume project memory without a rewrite. |

## Mental model

Persist durable facts, derive display status:

- session table: `activity_state`, `is_terminated`, identity, metadata
- PR tables: PR/CI/review facts
- derived read model: `service.Session` computes display status from session + PR facts
