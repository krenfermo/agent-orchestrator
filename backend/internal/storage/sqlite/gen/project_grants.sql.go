// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: project_grants.sql

package gen

import (
	"context"
	"time"

	"github.com/aoagents/agent-orchestrator/backend/internal/domain"
)

const deleteProjectGrant = `-- name: DeleteProjectGrant :execrows
DELETE FROM project_grants WHERE project_id = ? AND subject_kind = ? AND subject_id = ?
`

type DeleteProjectGrantParams struct {
	ProjectID   domain.ProjectID
	SubjectKind domain.GrantSubjectKind
	SubjectID   string
}

func (q *Queries) DeleteProjectGrant(ctx context.Context, arg DeleteProjectGrantParams) (int64, error) {
	result, err := q.db.ExecContext(ctx, deleteProjectGrant, arg.ProjectID, arg.SubjectKind, arg.SubjectID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const deleteProjectGrantsBySubject = `-- name: DeleteProjectGrantsBySubject :execrows
DELETE FROM project_grants WHERE subject_kind = ? AND subject_id = ?
`

type DeleteProjectGrantsBySubjectParams struct {
	SubjectKind domain.GrantSubjectKind
	SubjectID   string
}

func (q *Queries) DeleteProjectGrantsBySubject(ctx context.Context, arg DeleteProjectGrantsBySubjectParams) (int64, error) {
	result, err := q.db.ExecContext(ctx, deleteProjectGrantsBySubject, arg.SubjectKind, arg.SubjectID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const listProjectGrantsByProject = `-- name: ListProjectGrantsByProject :many
SELECT id, project_id, subject_kind, subject_id, role, created_at, updated_at, created_by
FROM project_grants WHERE project_id = ?
`

func (q *Queries) ListProjectGrantsByProject(ctx context.Context, projectID domain.ProjectID) ([]ProjectGrant, error) {
	rows, err := q.db.QueryContext(ctx, listProjectGrantsByProject, projectID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ProjectGrant{}
	for rows.Next() {
		var i ProjectGrant
		if err := rows.Scan(
			&i.ID,
			&i.ProjectID,
			&i.SubjectKind,
			&i.SubjectID,
			&i.Role,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.CreatedBy,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listProjectGrantsBySubject = `-- name: ListProjectGrantsBySubject :many
SELECT id, project_id, subject_kind, subject_id, role, created_at, updated_at, created_by
FROM project_grants WHERE subject_kind = ? AND subject_id = ?
`

type ListProjectGrantsBySubjectParams struct {
	SubjectKind domain.GrantSubjectKind
	SubjectID   string
}

func (q *Queries) ListProjectGrantsBySubject(ctx context.Context, arg ListProjectGrantsBySubjectParams) ([]ProjectGrant, error) {
	rows, err := q.db.QueryContext(ctx, listProjectGrantsBySubject, arg.SubjectKind, arg.SubjectID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ProjectGrant{}
	for rows.Next() {
		var i ProjectGrant
		if err := rows.Scan(
			&i.ID,
			&i.ProjectID,
			&i.SubjectKind,
			&i.SubjectID,
			&i.Role,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.CreatedBy,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertProjectGrant = `-- name: UpsertProjectGrant :one

INSERT INTO project_grants (id, project_id, subject_kind, subject_id, role, created_at, updated_at, created_by)
VALUES (?, ?, ?, ?, ?, ?, ?, ?)
ON CONFLICT (project_id, subject_kind, subject_id)
DO UPDATE SET role = excluded.role, updated_at = excluded.updated_at
RETURNING id, project_id, subject_kind, subject_id, role, created_at, updated_at, created_by
`

type UpsertProjectGrantParams struct {
	ID          string
	ProjectID   domain.ProjectID
	SubjectKind domain.GrantSubjectKind
	SubjectID   string
	Role        domain.ProjectRole
	CreatedAt   time.Time
	UpdatedAt   time.Time
	CreatedBy   domain.UserID
}

// P4-B: project-scoped access grants. A grant's subject is a user or a team;
// its object is exactly one project. No trailing ORDER BY/LIMIT on :many
// queries (see users.sql's sqlc note).
// UpsertProjectGrant is the only write path for a grant: re-granting a subject
// that already has access changes its role instead of creating a second row,
// which is what the (project_id, subject_kind, subject_id) unique index makes
// structurally true rather than a convention.
func (q *Queries) UpsertProjectGrant(ctx context.Context, arg UpsertProjectGrantParams) (ProjectGrant, error) {
	row := q.db.QueryRowContext(ctx, upsertProjectGrant,
		arg.ID,
		arg.ProjectID,
		arg.SubjectKind,
		arg.SubjectID,
		arg.Role,
		arg.CreatedAt,
		arg.UpdatedAt,
		arg.CreatedBy,
	)
	var i ProjectGrant
	err := row.Scan(
		&i.ID,
		&i.ProjectID,
		&i.SubjectKind,
		&i.SubjectID,
		&i.Role,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.CreatedBy,
	)
	return i, err
}
