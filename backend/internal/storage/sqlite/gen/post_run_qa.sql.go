// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: post_run_qa.sql

package gen

import (
	"context"
	"database/sql"
	"time"
)

const getLatestPostRunQARunForSubject = `-- name: GetLatestPostRunQARunForSubject :one
SELECT id, subject_kind, subject_id, phase, findings_json,
       repair_cycle_count, max_repair_cycles, result, started_at, completed_at
FROM post_run_qa_runs
WHERE subject_kind = ? AND subject_id = ?
ORDER BY started_at DESC, id DESC
LIMIT 1
`

type GetLatestPostRunQARunForSubjectParams struct {
	SubjectKind string
	SubjectID   string
}

// What a restarting daemon asks: is this subject's gate still open, and where
// did it get to? id breaks a started_at tie so the answer is deterministic.
func (q *Queries) GetLatestPostRunQARunForSubject(ctx context.Context, arg GetLatestPostRunQARunForSubjectParams) (PostRunQaRun, error) {
	row := q.db.QueryRowContext(ctx, getLatestPostRunQARunForSubject, arg.SubjectKind, arg.SubjectID)
	var i PostRunQaRun
	err := row.Scan(
		&i.ID,
		&i.SubjectKind,
		&i.SubjectID,
		&i.Phase,
		&i.FindingsJson,
		&i.RepairCycleCount,
		&i.MaxRepairCycles,
		&i.Result,
		&i.StartedAt,
		&i.CompletedAt,
	)
	return i, err
}

const getPostRunQARun = `-- name: GetPostRunQARun :one
SELECT id, subject_kind, subject_id, phase, findings_json,
       repair_cycle_count, max_repair_cycles, result, started_at, completed_at
FROM post_run_qa_runs
WHERE id = ?
`

func (q *Queries) GetPostRunQARun(ctx context.Context, id string) (PostRunQaRun, error) {
	row := q.db.QueryRowContext(ctx, getPostRunQARun, id)
	var i PostRunQaRun
	err := row.Scan(
		&i.ID,
		&i.SubjectKind,
		&i.SubjectID,
		&i.Phase,
		&i.FindingsJson,
		&i.RepairCycleCount,
		&i.MaxRepairCycles,
		&i.Result,
		&i.StartedAt,
		&i.CompletedAt,
	)
	return i, err
}

const upsertPostRunQARun = `-- name: UpsertPostRunQARun :one
INSERT INTO post_run_qa_runs (
    id, subject_kind, subject_id, phase, findings_json,
    repair_cycle_count, max_repair_cycles, result, started_at, completed_at
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
ON CONFLICT(id) DO UPDATE SET
    subject_kind = excluded.subject_kind,
    subject_id = excluded.subject_id,
    phase = excluded.phase,
    findings_json = excluded.findings_json,
    repair_cycle_count = excluded.repair_cycle_count,
    max_repair_cycles = excluded.max_repair_cycles,
    result = excluded.result,
    started_at = excluded.started_at,
    completed_at = excluded.completed_at
RETURNING id, subject_kind, subject_id, phase, findings_json,
          repair_cycle_count, max_repair_cycles, result, started_at, completed_at
`

type UpsertPostRunQARunParams struct {
	ID               string
	SubjectKind      string
	SubjectID        string
	Phase            string
	FindingsJson     string
	RepairCycleCount int64
	MaxRepairCycles  int64
	Result           string
	StartedAt        time.Time
	CompletedAt      sql.NullTime
}

// Save one pass of the Post-Run QA gate. Keyed on the run's own id, so
// advancing a live pass (phase, findings, repair cycle, verdict) rewrites its
// row while a subject's earlier passes are left untouched.
func (q *Queries) UpsertPostRunQARun(ctx context.Context, arg UpsertPostRunQARunParams) (PostRunQaRun, error) {
	row := q.db.QueryRowContext(ctx, upsertPostRunQARun,
		arg.ID,
		arg.SubjectKind,
		arg.SubjectID,
		arg.Phase,
		arg.FindingsJson,
		arg.RepairCycleCount,
		arg.MaxRepairCycles,
		arg.Result,
		arg.StartedAt,
		arg.CompletedAt,
	)
	var i PostRunQaRun
	err := row.Scan(
		&i.ID,
		&i.SubjectKind,
		&i.SubjectID,
		&i.Phase,
		&i.FindingsJson,
		&i.RepairCycleCount,
		&i.MaxRepairCycles,
		&i.Result,
		&i.StartedAt,
		&i.CompletedAt,
	)
	return i, err
}
