// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: skill_images.sql

package gen

import (
	"context"
	"database/sql"
	"time"

	"github.com/aoagents/agent-orchestrator/backend/internal/domain"
)

const getSkillImageApprovalByID = `-- name: GetSkillImageApprovalByID :one
SELECT id, tenant_id, project_id, skill_id, version, mode_id, tool,
    reference, digest, approved_by, approved_at, expires_at, revoked_at, note
FROM skill_image_approvals WHERE id = ?
`

func (q *Queries) GetSkillImageApprovalByID(ctx context.Context, id string) (SkillImageApproval, error) {
	row := q.db.QueryRowContext(ctx, getSkillImageApprovalByID, id)
	var i SkillImageApproval
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.ProjectID,
		&i.SkillID,
		&i.Version,
		&i.ModeID,
		&i.Tool,
		&i.Reference,
		&i.Digest,
		&i.ApprovedBy,
		&i.ApprovedAt,
		&i.ExpiresAt,
		&i.RevokedAt,
		&i.Note,
	)
	return i, err
}

const getSkillImageApprovalForScope = `-- name: GetSkillImageApprovalForScope :one
SELECT id, tenant_id, project_id, skill_id, version, mode_id, tool,
    reference, digest, approved_by, approved_at, expires_at, revoked_at, note
FROM skill_image_approvals
WHERE tenant_id = ? AND project_id = ? AND skill_id = ? AND version = ?
    AND mode_id = ? AND tool = ?
`

type GetSkillImageApprovalForScopeParams struct {
	TenantID  domain.TenantID
	ProjectID domain.ProjectID
	SkillID   string
	Version   string
	ModeID    string
	Tool      string
}

func (q *Queries) GetSkillImageApprovalForScope(ctx context.Context, arg GetSkillImageApprovalForScopeParams) (SkillImageApproval, error) {
	row := q.db.QueryRowContext(ctx, getSkillImageApprovalForScope,
		arg.TenantID,
		arg.ProjectID,
		arg.SkillID,
		arg.Version,
		arg.ModeID,
		arg.Tool,
	)
	var i SkillImageApproval
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.ProjectID,
		&i.SkillID,
		&i.Version,
		&i.ModeID,
		&i.Tool,
		&i.Reference,
		&i.Digest,
		&i.ApprovedBy,
		&i.ApprovedAt,
		&i.ExpiresAt,
		&i.RevokedAt,
		&i.Note,
	)
	return i, err
}

const listSkillImageApprovals = `-- name: ListSkillImageApprovals :many
SELECT id, tenant_id, project_id, skill_id, version, mode_id, tool,
    reference, digest, approved_by, approved_at, expires_at, revoked_at, note
FROM skill_image_approvals
`

func (q *Queries) ListSkillImageApprovals(ctx context.Context) ([]SkillImageApproval, error) {
	rows, err := q.db.QueryContext(ctx, listSkillImageApprovals)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SkillImageApproval{}
	for rows.Next() {
		var i SkillImageApproval
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.ProjectID,
			&i.SkillID,
			&i.Version,
			&i.ModeID,
			&i.Tool,
			&i.Reference,
			&i.Digest,
			&i.ApprovedBy,
			&i.ApprovedAt,
			&i.ExpiresAt,
			&i.RevokedAt,
			&i.Note,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSkillImageApprovalsForProject = `-- name: ListSkillImageApprovalsForProject :many
SELECT id, tenant_id, project_id, skill_id, version, mode_id, tool,
    reference, digest, approved_by, approved_at, expires_at, revoked_at, note
FROM skill_image_approvals WHERE project_id = ?
`

func (q *Queries) ListSkillImageApprovalsForProject(ctx context.Context, projectID domain.ProjectID) ([]SkillImageApproval, error) {
	rows, err := q.db.QueryContext(ctx, listSkillImageApprovalsForProject, projectID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SkillImageApproval{}
	for rows.Next() {
		var i SkillImageApproval
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.ProjectID,
			&i.SkillID,
			&i.Version,
			&i.ModeID,
			&i.Tool,
			&i.Reference,
			&i.Digest,
			&i.ApprovedBy,
			&i.ApprovedAt,
			&i.ExpiresAt,
			&i.RevokedAt,
			&i.Note,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const revokeSkillImageApproval = `-- name: RevokeSkillImageApproval :execrows
UPDATE skill_image_approvals SET revoked_at = ?
WHERE id = ? AND revoked_at IS NULL
`

type RevokeSkillImageApprovalParams struct {
	RevokedAt sql.NullTime
	ID        string
}

func (q *Queries) RevokeSkillImageApproval(ctx context.Context, arg RevokeSkillImageApprovalParams) (int64, error) {
	result, err := q.db.ExecContext(ctx, revokeSkillImageApproval, arg.RevokedAt, arg.ID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}

const upsertSkillImageApproval = `-- name: UpsertSkillImageApproval :one

INSERT INTO skill_image_approvals (
    id, tenant_id, project_id, skill_id, version, mode_id, tool,
    reference, digest, approved_by, approved_at, expires_at, revoked_at, note
)
VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
ON CONFLICT (tenant_id, project_id, skill_id, version, mode_id, tool) DO UPDATE SET
    reference = excluded.reference,
    digest = excluded.digest,
    approved_by = excluded.approved_by,
    approved_at = excluded.approved_at,
    expires_at = excluded.expires_at,
    revoked_at = excluded.revoked_at,
    note = excluded.note
RETURNING id, tenant_id, project_id, skill_id, version, mode_id, tool,
    reference, digest, approved_by, approved_at, expires_at, revoked_at, note
`

type UpsertSkillImageApprovalParams struct {
	ID         string
	TenantID   domain.TenantID
	ProjectID  domain.ProjectID
	SkillID    string
	Version    string
	ModeID     string
	Tool       string
	Reference  string
	Digest     string
	ApprovedBy string
	ApprovedAt time.Time
	ExpiresAt  sql.NullTime
	RevokedAt  sql.NullTime
	Note       string
}

// Skills phase 8: image approvals, the trust root for what AO may execute.
// No trailing ORDER BY/LIMIT -- see users.sql's note on the sqlc v1.31.1 SQLite
// codegen bug; sort in Go.
func (q *Queries) UpsertSkillImageApproval(ctx context.Context, arg UpsertSkillImageApprovalParams) (SkillImageApproval, error) {
	row := q.db.QueryRowContext(ctx, upsertSkillImageApproval,
		arg.ID,
		arg.TenantID,
		arg.ProjectID,
		arg.SkillID,
		arg.Version,
		arg.ModeID,
		arg.Tool,
		arg.Reference,
		arg.Digest,
		arg.ApprovedBy,
		arg.ApprovedAt,
		arg.ExpiresAt,
		arg.RevokedAt,
		arg.Note,
	)
	var i SkillImageApproval
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.ProjectID,
		&i.SkillID,
		&i.Version,
		&i.ModeID,
		&i.Tool,
		&i.Reference,
		&i.Digest,
		&i.ApprovedBy,
		&i.ApprovedAt,
		&i.ExpiresAt,
		&i.RevokedAt,
		&i.Note,
	)
	return i, err
}
